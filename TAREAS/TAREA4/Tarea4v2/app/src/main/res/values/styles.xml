<?xml version="1.0" encoding="utf-8"?>
<resources>

    <style name="SpinnerStyle"  parent="Widget.AppCompat.Spinner">
        <item name="android:background">@android:color/transparent</item> <!-- Fondo transparente -->
        <item name="android:popupBackground">@android:color/white</item> <!-- Fondo blanco para el desplegable -->
        <item name="android:dropDownSelector">@android:color/white</item> <!-- Selector de elementos del desplegable -->
        <item name="android:textColor">@color/white</item>
        <item name="textInputStyle">@color/white</item>

    </style>
</resources>